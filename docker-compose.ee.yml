services:
  nekoclaw-backend:
    volumes:
      - ./ee:/app/ee:ro

  nekoclaw-admin:
    build:
      context: ee/nekoclaw-frontend
    depends_on:
      - nekoclaw-backend
    ports:
      - "${ADMIN_PORT:-8001}:80"
    restart: unless-stopped
