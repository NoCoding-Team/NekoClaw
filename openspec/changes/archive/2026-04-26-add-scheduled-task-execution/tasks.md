## 1. 数据模型与迁移

- [x] 1.1 统一定时任务 ID 类型为字符串 UUID，修正后端 schema、API 参数和前端类型
- [x] 1.2 扩展 `ScheduledTask` 字段：`schedule_type`、`timezone`、`status`、`allowed_tools`、`last_status`、`missed_count`
- [x] 1.3 新增 `ScheduledTaskRun` 模型，记录任务执行历史、关联会话、错误信息和工具权限快照
- [x] 1.4 编写启动迁移/兼容逻辑，为已有任务补齐新增字段并计算 `next_run_at`

## 2. 后端 API 与调度状态

- [x] 2.1 更新定时任务创建/编辑接口，校验 once/cron 配置并保存 `allowed_tools`
- [x] 2.2 增加后端 `next_run_at` 计算逻辑，创建/编辑/执行后保持任务下次触发时间准确
- [x] 2.3 增加执行历史列表接口，支持按任务查看历次执行记录
- [x] 2.4 增加执行记录创建、成功、失败、错过和忽略的状态回写接口
- [x] 2.5 调整立即执行接口，使其创建执行记录并复用自动触发执行链路

## 3. 桌面端调度接入

- [x] 3.1 更新 Electron scheduler 类型为字符串 ID，并同步 `allowed_tools`、`next_run_at`、`timezone`
- [x] 3.2 在任务列表拉取/创建/编辑/删除/暂停后同步 enabled 任务到 Electron scheduler
- [x] 3.3 监听 `scheduler:fired` 事件，触发渲染进程执行任务
- [x] 3.4 桌面端启动或同步时检测错过任务：一次性任务提示补跑/忽略，周期任务记录 missed 并跳到下一次

## 4. 前端执行闭环

- [x] 4.1 实现定时任务触发后自动创建新会话，并将任务描述作为用户消息发送
- [x] 4.2 发送 WebSocket 消息时携带任务保存的 `allowed_tools`
- [x] 4.3 将执行记录与创建的会话 ID 关联，并在 Agent 完成/失败时回写状态
- [x] 4.4 更新任务面板展示最近状态、下次触发时间、工具权限摘要和执行历史入口
- [x] 4.5 在创建/编辑表单中保存当前爪力配置为 `allowed_tools` 快照，并向用户展示将授予的工具

## 5. 验证

- [ ] 5.1 验证一次性任务到点后创建新会话并按保存的工具权限执行
- [ ] 5.2 验证周期任务按 cron 重复触发，离线错过后只记录 missed 不自动补跑
- [ ] 5.3 验证修改全局爪力配置不会影响已保存任务的 `allowed_tools`
- [ ] 5.4 验证立即执行、暂停/恢复、删除和执行历史展示
- [x] 5.5 运行后端与桌面端相关类型检查/测试，确认无新增 lint 或类型错误
