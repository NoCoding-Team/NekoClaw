## 1. 会话来源与记忆策略

- [x] 1.1 为会话模型/API 增加 `source` 与 `memory_policy` 字段，旧会话默认 `chat` / `auto`
- [x] 1.2 增加启动迁移，为已有 `sessions` 表补齐来源和记忆策略字段
- [x] 1.3 定时任务创建会话时写入 `source=scheduled_task` 与默认只读记忆策略
- [x] 1.4 WebSocket 消息发送链路支持传递定时任务执行上下文和记忆策略

## 2. 定时任务记忆隔离

- [x] 2.1 为定时任务执行构建专用提示词，说明计划任务结果默认不写入长期记忆
- [x] 2.2 `memory_refresh()` 在触发前检查会话 `memory_policy`，跳过只读/不写入会话
- [x] 2.3 每日笔记生成查询消息时排除只读定时任务会话
- [x] 2.4 Daily Digest 提示词明确将定时任务临时输出视为低价值内容，除非明确要求记住

## 3. 每日笔记按需加载

- [x] 3.1 调整 `_load_memory()`，移除今日/昨日 daily notes 全文默认注入
- [x] 3.2 当 query_hint 指向近期记录或历史主题时，通过 `search_memory`/RAG 检索 daily note 相关片段
- [x] 3.3 明确指定日期或文件路径时，允许 Agent 使用 `memory_read` 读取对应 daily note
- [x] 3.4 确认记忆面板列表接口只返回元数据，点击文件后才读取正文

## 4. 记忆工具策略

- [x] 4.1 更新工具定义，明确 `search_memory` 是历史记忆和每日笔记的默认查询入口
- [x] 4.2 更新 `memory_read` 描述，仅用于明确文件读取或写入前读取旧内容
- [x] 4.3 更新 `memory_write` 描述，仅用于明确保存、长期事实、重要决策或整理后写回完整文件
- [x] 4.4 更新系统提示词中的记忆策略，避免模型在普通查询时突兀调用 `memory_read`

## 5. 验证

- [ ] 5.1 验证天气预报类定时任务不会进入 memory refresh、每日笔记和 MEMORY.md
- [ ] 5.2 验证明确要求记住的定时任务仍可写入记忆
- [ ] 5.3 验证正常对话不再默认注入今日/昨日笔记全文
- [ ] 5.4 验证询问历史记录时优先使用 `search_memory`，明确读取文件时才使用 `memory_read`
- [x] 5.5 运行后端与桌面端相关类型检查/测试
